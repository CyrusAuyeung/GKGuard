title: "Architecture and integration discussion"
labels: ["architecture", "integration"]
body:
  - type: textarea
    id: topic
    attributes:
      label: Topic / 讨论主题
      description: What architecture, maintenance, or integration topic should be discussed?
    validations:
      required: true
  - type: checkboxes
    id: scope
    attributes:
      label: Scope / 相关范围
      options:
        - label: GKGuard C2 backend / GKGuard C2 后端
        - label: GKGuard C2 frontend / GKGuard C2 前端
        - label: Electron desktop app / Electron 桌面端
        - label: CampusVision C1 service / CampusVision C1 服务
        - label: CampusCar / UE / control team
        - label: Docs / Release / maintenance
  - type: textarea
    id: background
    attributes:
      label: Background and constraints / 背景与约束
    validations:
      required: true
  - type: textarea
    id: decisions
    attributes:
      label: Decisions needed / 需要决定的问题
  - type: checkboxes
    id: data_boundary
    attributes:
      label: Security and data boundary / 安全与数据边界
      options:
        - label: This does not expose real media, personal data, server passwords, tokens, or private keys. / 不暴露真实媒体、个人信息、服务器密码、token 或私钥。
        - label: If this changes behavior, related docs and Release notes will be updated. / 如改变行为，会同步相关文档和 Release notes。
