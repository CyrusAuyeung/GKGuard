<p align="right">
  <a href="#中文"><kbd>中文</kbd></a>
  <a href="#english"><kbd>English</kbd></a>
</p>

<a id="中文"></a>

# GKGuard v0.1.18

## 更新重点

- 统一仓库文档、治理文档、GitHub 模板和历史发布说明中的 CampusVision C1 服务、GKGuard C2 工作台、GKGuard C2 本地代理层表述，减少读者对 CampusVision C1 / GKGuard C2 边界的猜测。
- 将已发布 GitHub Release 正文同步为仓库内维护的新版 `docs/releases/v*.md` 内容，使线上发布说明与仓库记录保持一致。
- 将桌面端 CampusVision C1 SSH 密码窗口的可见文案从短称改为 `CampusVision C1 服务`，与 README 和集成文档保持一致。
- 新增 markdownlint 配置，承认仓库现有双语 HTML 导航和表格样式，清理 VS Code 中的文档风格告警而不重写既有正文。

## 验证

- 通过：`python -m pytest backend`，24 passed，1 个上游 deprecation warning。
- 通过：`node --check desktop/main.js` 和 `node --check desktop/preload.js`。
- 通过：`python -m compileall backend`。
- 通过：`npm audit --audit-level=low`，0 vulnerabilities。
- 通过：`git diff --check`。
- 通过：线上 GitHub Release 抽查，确认历史发布正文已包含 CampusVision C1 / GKGuard C2 清晰表述。

## 发布文件

- `GKGuard-Setup-0.1.18.exe`：Windows 桌面安装包。
- `GKGuard-Setup-0.1.18.exe.blockmap`：Electron 更新差分元数据。
- `latest.yml`：Electron 自动更新元数据。

普通首次安装下载并运行 `GKGuard-Setup-0.1.18.exe`。

<p align="right"><a href="#中文">返回中文顶部</a></p>

---

<a id="english"></a>

# GKGuard v0.1.18

## Highlights

- Standardized the wording for the CampusVision C1 service, GKGuard C2 workbench, and GKGuard C2 local proxy layer across repository documentation, governance files, GitHub templates, and historical Release notes so readers do not need to infer the CampusVision C1 / GKGuard C2 boundary.
- Synchronized the already-published GitHub Release bodies with the maintained `docs/releases/v*.md` files so the online Release notes match the repository record.
- Updated the visible desktop CampusVision C1 SSH password prompt from the short label to `CampusVision C1 服务`, matching README and integration documentation terminology.
- Added markdownlint configuration for the repository's existing bilingual HTML navigation and table style, clearing VS Code documentation style warnings without rewriting existing prose.

## Verification

- Passed: `python -m pytest backend`, 24 passed with 1 upstream deprecation warning.
- Passed: `node --check desktop/main.js` and `node --check desktop/preload.js`.
- Passed: `python -m compileall backend`.
- Passed: `npm audit --audit-level=low`, 0 vulnerabilities.
- Passed: `git diff --check`.
- Passed: online Release spot checks confirming historical Release bodies now contain clear CampusVision C1 / GKGuard C2 wording.

## Release Assets

- `GKGuard-Setup-0.1.18.exe`: Windows desktop installer.
- `GKGuard-Setup-0.1.18.exe.blockmap`: Electron differential update metadata.
- `latest.yml`: Electron auto-update metadata.

For a first-time installation, download and run `GKGuard-Setup-0.1.18.exe`.
